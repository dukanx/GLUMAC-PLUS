package com.example.glumac_plus_android.ui.theme

import androidx.compose.ui.graphics.Color

// "Calm Editorial" paleta — topla bež (paper) pozadina, taman (ink) tekst, rust akcent.
val Paper = Color(0xFFEFE7D8)        // pozadina — vidljivo bež
val PaperCard = Color(0xFFF6EFE1)    // kartice (svetlije od pozadine)
val PaperCardHigh = Color(0xFFECE3D0)
val Paper2 = Color(0xFFE6DCC9)       // chip/varijanta
val Ink = Color(0xFF2A1E14)          // tekst — tamno, toplo (skoro crno)
val InkMuted = Color(0xFF6E5D50)     // prigušeni tekst / outline
val Rust = Color(0xFFB94927)         // akcent (dugmad, aktivno)
val PaperOnRust = Color(0xFFFDFBF5)  // tekst na rust dugmadima
val GreskaRed = Color(0xFFB3261E)
